# Project audit — 2026-07-30

Tree audited at `ca8b5c7`, working tree clean. Suite re-run from a cold
server state: **no failures, 2 skips**, ~54 s.

## What "every function" actually means here

The tree holds 199,164 lines, but 130,462 of those are
`assistant/knowledge/user_library/` — imported third-party reference material
(SigmaHQ rules, TheAlgorithms, kernel docs), not this project's code. The
audited surface is **160 Python files** of project code.

Every one of those 160 files was parsed and every function in them checked
mechanically for the defect classes below. Functions on the authority,
release, and probe paths were additionally read by hand. Individual line-by-
line reading of all ~2,000 callables did not happen and is not what produced
these findings.

| package | files | loc | callables |
| --- | --- | --- | --- |
| tools | 38 | 13,257 | 348 |
| assistant/core | 23 | 9,085 | 282 |
| assistant/visualizer | 18 | 6,129 | 191 |
| assistant/commands | 3 | 4,718 | 156 |
| assistant/ui | 3 | 4,298 | 149 |
| assistant/editing | 16 | 4,070 | 114 |
| assistant/(root) | 3 | 3,513 | 79 |
| assistant/voice | 4 | 3,063 | 82 |
| assistant/memory | 10 | 2,185 | 71 |
| assistant/hardware | 3 | 1,321 | 50 |
| assistant/project | 4 | 565 | 15 |
| assistant/web | 5 | 403 | 7 |

## Clean

Stated first because it is the larger result.

- **0 syntax errors** across all 160 files.
- **0 bare `except:`** in non-test code.
- **0 mutable default arguments.**
- **0 TODO / FIXME / XXX / HACK markers.**
- **0 invalid escape sequences** in project code.
- The researchB rename verifies complete and independently: every remaining
  `researchA` string in `tools/` is either the documented `COMBINED_FORMAT`
  decision, a historical doc that must ship, or an explanatory comment.
- `llm_server.is_alive()` and `embedding_server.is_alive()` are both correct
  — they probe `/v1/models` with authentication and bind the running server
  to an expected model alias.

---

## 1. `machinespirit.diagnose()` reports `ready: True` with no servers running

**FIXED 2026-07-30.** Resolution and its verification are at the end of this
section; the finding is kept in full because the reasoning is what stops it
being reintroduced.

**Confirmed by reproduction. Highest-value finding.**

Two independent flaws in the function whose entire documented purpose is
"Which part is missing, rather than that something is."

**a. The unpooled probe accepts any HTTP 200.** `available()`
(`core/machinespirit.py:146`) reads `/health`. llama.cpp does not
authenticate that route, so a bad key still returns 200 and any unrelated
server satisfies it.

**b. The pooled probe never touches the network.**
`diagnose()["pooled"]` (`core/machinespirit.py:339`) calls
`embedding_server.available()`, which is
`EMBED_ENABLED and os.path.isfile(EMBED_MODEL_PATH) and _url_is_loopback(...)`
— a configuration check. It is True whenever the model *file* exists.

Measured with **no llama-server running at all**:

```
embedding_server.available()       = True     <- becomes diagnose()["pooled"]
embedding_server._health_responds()= False
embedding_server.is_alive()        = False    <- the correct check, unused here
```

And with a throwaway server on 8084 answering `/health` with 200 and 401ing
everything else — the shape of the stale `sable-anchor-field-check` server
that cost an hour on 2026-07-29:

```
machinespirit.available()       = True
machinespirit.trajectory('hello') = None
diagnose() -> configured True, unpooled True, pooled True, ready True, reason None
```

`ready: True`, `reason: None`, and the caller gets `None`.

**Confirmed live in Super Dev Hazard**, against the real unpooled server on
8084 and the real anchor dictionary — not a stub. Baseline with both
embedders up, tracing "a chirping carbon monoxide alarm":

```
trajectory()     : 10 tokens
anchor_vectors() : 184 anchors
readout          : 'a fire alarm during an exam' 0.374 @8;
                   'the silence after a loud noise' 0.305 @3
```

The pooled server on 8082 was then killed, leaving 8084 up:

```
pooled 8082 is_alive()   = False      <- it is genuinely gone
pooled 8082 available()  = True       <- what diagnose() reads
diagnose() -> configured True, unpooled True, pooled True,
              tokens 10, ready True, reason None
anchor_vectors()         = None
readout                  = (empty)
```

This is worse than the stub case. The unpooled half still works, so
`diagnose(text=...)` reports `tokens: 10` — which reads as positive evidence
that the pipeline is healthy — alongside `ready: True` and `reason: None`,
while the trace produces nothing at all. The single honest signal,
`anchor_vectors()` returning `None`, is silent by design.

It lands exactly where `anchor_vectors():202` predicted in its own docstring:
"A trace that fails because the pooled embedder is absent looks identical to
one that fails because the unpooled one is." `diagnose()` was written to
resolve that ambiguity and does not.

The consequence is inverted messaging. The branch at `:364` that says *"the
ordinary embedding server is not [running]"* can only fire when the model
**file** is missing — a configuration problem. During a real pooled-server
outage `status["pooled"]` stays True, the branch is skipped, and `ready`
becomes True. The docstring warns that "reporting a pooled-server outage as
an unpooled one sends the operator to restart the wrong process"; the code
reports it as no problem at all.

**Fix**: `diagnose()` should call `embedding_server.is_alive()` for the
pooled half, and `available()` should verify an authenticated route and the
`--pooling none` identity, mirroring `embedding_server.is_alive():170`.
The pattern is already written, one module over.

Carried unfixed across two handoffs. Repro script preserved in the session
scratchpad as `repro_8084.py`.

### Resolution

Both halves now use the pattern the sibling modules already had.

- `available()` probes `/v1/models` **with the key** instead of `/health`,
  and requires the response to carry at least one model id. It does not
  claim to verify `--pooling none`; there is no route that reports pooling,
  and the docstring says so rather than implying a guarantee it cannot make.
- `diagnose()["pooled"]` reads `embedding_server.is_alive()`. It is still
  computed before the `configured()` early return, because `reconstruct`
  reads that field alone and needs it meaningful in ordinary mode.
- The reason text now separates a stopped process from an absent model, so
  the repair it names is the repair that is needed.

**Eight tests added**, in `DiagnoseTests` and a new `UnpooledProbeTests`.
Verified to fail against the unfixed source — reverting `machinespirit.py`
alone produces 5 failures, including `assertFalse(ms.available())` raising
`True is not false` for the health-only squatter.

The existing `test_pooled_down_is_reported_as_the_pooled_one`, whose
docstring called it "the regression this function exists for", was passing
because its helper stubbed `embedding_server.available()` — the
configuration check. It simulated a missing model file and called it an
outage, so the test carried the same conflation as the code. The helper now
stubs liveness and configuration separately.

Re-verified live, against the real servers rather than a stub:

| state | before | after |
| --- | --- | --- |
| both up | `ready True` | `ready True`, identical readout and scores |
| pooled 8082 killed | `pooled True, tokens 10, ready True, reason None` | `pooled False, ready False`, reason names a *stopped server rather than a missing file* |

Suite: no failures, 2 skips.

## 2. The regression suite starts a real llama-server and leaves it running

**Confirmed.** Chain:

`tests/test_regressions.py:3007` calls `assistant_main.main()` →
`main.py:3237` spawns `threading.Thread(target=_start_semantic_layer, daemon=True)`
→ `_start_semantic_layer` (`main.py:680`) → `embedding_server.start()`
(`main.py:693`) → a real `llama-server.exe`.

Verified by killing every llama-server, running the suite, and finding a new
process created 09:43:20 — mid-run — still alive after the runner exited 0.
`run_regressions.py` does nothing after the tests; the process is a leak.

The same thread also reaches `semantic_index.start_worker()`,
`history_recall.refresh()` and `knowledge_library.request_embedding()`
against real state.

This is the family the previous session documented: a test touching a live
subsystem. That test is otherwise careful — it patches `start_server`,
`_prepare_voice_for_startup`, `memory_worker`, and `_record_session_rhythm`,
with a comment explaining that the last one would otherwise file the suite's
duration as one of the operator's sessions. The background thread was missed.

Consequences: the suite is not hermetic, a server accumulates on each run,
and successive runs take a different branch (`is_alive()` True) than the
first — so the suite is order-dependent on its own history.

**Fix**: patch the thread spawn, or `_start_semantic_layer`, in that test.

## 3. The edit-guard deny lists fail open on rename

**Latent, security-relevant, not currently triggered.**

`DENIED_FILES` (13), `MAINTENANCE_DENIED_FILES` (13) and
`AUTONOMOUS_ALLOWED_FILES` (5) in `editing/edit_guard.py` name files by path.
**All 31 exist today. No test asserts that they do.**

`resolve():322` denies by exact name match; `list_editable_files():353`
includes any file `resolve()` does not refuse. So renaming a protected file
silently removes its protection and widens the editable surface — the deny
list fails **open**.

The files behind those entries are `core/persona.py`, `core/dev_auth.py`,
`core/agent_interface.py`, `main.py`, `commands/command_handlers.py` and
similar. The module's own comment says a constraint the constrained thing can
edit is decoration; a rename makes it decoration silently.

`list_autonomous_files():363` intersects the allowlist with the editable set,
so the unattended path fails closed. Only the human-confirm and maintenance
surfaces are exposed.

**Fix**: the test the last session wrote for `INCLUDE_FILES` —
`test_every_whitelisted_file_actually_exists` — applied to these four lists.

## 4. Agent-interface audit logging fails silently

`core/agent_interface.py:130` documents that "an interface that can be
queried without leaving a trace is one nobody can audit afterwards, which is
the property that made autonomous edits acceptable in the first place."

`:143` is `except OSError: pass`. An unwritable path or a full disk means
calls proceed and are not recorded, with nothing reported. The stated
justification for the feature is the thing being discarded quietly.

## 5. Line endings — the foot-gun's root cause, measured

`core.autocrlf=false` and **no `.gitattributes`**, so whatever is written is
committed.

- **15 files are CRLF**, including `ui/ui.py`, `commands/command_handlers.py`,
  `core/tutorial.py`, `core/dev_auth.py`, `editing/super_dev_engine.py`.
- **3 files carry a UTF-8 BOM**: `tools/machinesoul.py`,
  `tests/test_machinesoul.py`, `tests/test_machinespirit.py`.
- **3 files are committed with mixed endings inside one file** —
  `editing/approval_manager.py` (`i/mixed`), `editing/change_planner.py`,
  `project/project_analyzer.py`.

Mixed endings within a single tracked file are the fingerprint of exactly the
scripted-rewrite accident the handoff records. A `.gitattributes` pinning
`*.py text eol=lf` would end the whole class.

## 6. Dead code

- `voice/offline_voice.py:2756`, `:2760`, `:2764` —
  `_load_daisy_cache`, `_save_daisy_cache`, `_build_daisy_audio`. Private
  methods documented as "backward-compatible wrapper", with no callers
  anywhere including the suite. A private method has no external contract to
  preserve. (`sing_daisy_bell` at `:2768` *is* live — `main.py:2428`.)
- `tools/glitch_icon.py:406` — `_swap()`, a one-line wrapper; only
  `_swap_by_identity()` is ever called.
- 11 unused imports. `offline_voice.py:109` is *not* among them — it is a
  deliberate availability probe marked `noqa: F401`.

## 7. Release-path observations

- `INCLUDE_DIRS` is **not** covered by the new existence test; only
  `INCLUDE_FILES` is. Two of its five entries (`assistant`, `icon_anim`) are
  repo-resident and checkable by the same means.
- `tools/package_release.py:258` sweeps the repo root at import time and adds
  any `.md`/`.txt` whose name contains SAFETY, PRIVACY, MODEL, NOTICE,
  RIGHTS, CONTRIBUT or SECURITY. A stray root note matching one of those
  words would ship without appearing in any whitelist a human reviews. **No
  live leak today** — the sweep currently adds nothing beyond the explicit
  list. Worth knowing before a cut.

---

## 8. Super Dev Hazard cannot fit its own prompt, and blames the operator

**FIXED 2026-07-30** — resolution at the end of this section.

**Found by the operator during interface-level testing, 2026-07-30.
Reproduced and measured. This one is release-relevant.**

Observed live: after the idle check ("Closing in 60s unless you say
something"), a 13-word reply came back as

> AI request failed: This message and its required instructions do not fit
> the 4096-token context window. Shorten the message or split it into
> smaller parts.

`start_super_dev_hazard.bat:72` sets `TORMENT_NEXUS_CONTEXT_SIZE=4096`, half
the 8192 default, to match the 14B planner's `-c 4096`. Nothing else was
resized to match. Measured with that exact environment and that exact
message:

| component | tokens | sheddable? |
| --- | --- | --- |
| stable system prompt (persona, core memory, rules) | 2,335 | no |
| runtime context (clock, activity, session) | 145 | no |
| persona few-shots + boundary | 178 | no |
| offline-reference block (3 documents) | 1,289 | **no** |
| what the operator actually typed | 18 | — |
| **fixed floor** | **4,028** | |
| budget (`4096 - 420 - 64`) | 3,612 | |
| headroom for conversation history | **−416** | |

It is 416 tokens over **with zero history**, and the message it tells the
operator to shorten is 18 tokens — 0.4% of the prompt. The persona and core
memory alone take 65% of the budget.

Two distinct defects:

**a. The shedding loop cannot shed the largest variable item.**
`build_messages` (`main.py:1299`) drops conversation turns, then shrinks
`effective_search`, then raises. But `knowledge_block` is folded into
`operator_message` at `:1297`, *before* the loop, and is never reduced. Web
search context is bounded and truncated (`:1319`); the offline library block
is not. So the one component that scales with the question is the one the
budget cannot touch.

**b. The error names the wrong cause.** The operator's message is not why the
prompt did not fit, and shortening it cannot help — at 18 tokens there is
nothing to shorten. This is the same failure mode as finding 1: a diagnostic
that sends the operator to the wrong repair.

It is intermittent rather than constant because `is_transient_query()` skips
retrieval for greetings and acknowledgements. A turn that retrieves nothing
fits comfortably; a turn that retrieves three documents does not. That makes
it look random from the outside, which is the worst version of it.

Ordinary mode has 7,708 tokens of budget and the same floor, so it has
roughly 3,700 tokens of headroom and does not show this.

Candidate fixes, smallest first:

1. Shed the knowledge block in the loop the way `effective_search` already
   is, recomputing `operator_message` each pass. Fixes the failure.
2. Make the error state the real proportions rather than blaming the
   message. Does not fix the failure, but stops it misdirecting.
3. Raise Super Dev's `CONTEXT_SIZE` and the planner's `-c` together. Costs
   KV cache against the 14B's footprint, which is presumably why it is 4096.

1 and 2 together are the honest minimum. 3 is a capacity decision, not a
bug fix.

### Resolution — fixed 2026-07-30

Both 1 and 2. Option 3 was left alone: it is a RAM decision about the 14B,
and nothing here should make it quietly.

The budget loop now sheds retrieved documents after it has dropped history
and truncated web results, by **asking the library for fewer** rather than
truncating the block. Whole records, for two reasons: a half-quoted manual is
worse evidence than an absent one, because the missing half is invisible to
the reader; and re-fetching returns the matching citation list, so the
receipt keeps naming exactly what the model was shown. Cutting the string
would have left a receipt describing a document that was only partly there —
the failure the receipt layer exists to prevent.

The shedding is **additive**. It runs only where the old code had already
given up and raised, so no turn that previously succeeded takes a different
path.

The error, when even an empty block does not fit, now reports the measured
proportions, says what has already been dropped, and names raising
`CONTEXT_SIZE` or trimming core memory as the repair. It no longer mentions
shortening the message.

Verified live, same environment and the same message that failed:

| | before | after |
| --- | --- | --- |
| prompt | 4,028 tokens vs 3,612 — **raised** | 3,487 vs 3,612, 125 headroom |
| documents | 3 requested, none sheddable | 3 requested, **2 kept** |
| citations | — | every held citation present in what was shown |

Four tests in `PromptEfficiencyTests`, checked against the unfixed source:
three of them raise the operator's original error verbatim without it, and
the fourth catches the misleading wording. Suite: no failures, 2 skips.

Note it kept two of three documents rather than discarding the block. Shedding
to the budget instead of dropping wholesale is the difference between an
answer with less support and an answer with none.

---

## Not defects, checked and cleared

- The production-startup lines in suite output (`Reusing the model server
  already running.`, `Preparing offline voice interface...`) are stdout noise
  from properly-mocked unit tests, not live startup.
- `main.py:2345` swallowing an exception is correct — it is a `finally`
  teardown path where the radio may already be gone.
- 78 exception handlers returning `pass`/`None` were flagged mechanically;
  the sample read was largely deliberate and documented. Only `_log_call`
  (finding 4) contradicts its own stated contract. The remainder are not
  claimed as defects here — that list is a starting point, not a verdict.

## Suggested order

1. ~~Finding 1~~ — **done 2026-07-30**, verified live and by test.
2. ~~Finding 8~~ — **done 2026-07-30**, verified live and by test.
3. Finding 2 — one `mock.patch` and the suite stops leaking processes.
3. Finding 3 — one test, same shape as the one already written.
4. Finding 5 — a `.gitattributes` retires a whole class of accident.

Findings 4, 6 and 7 are real but can wait.
